[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rofi-agent-picker"
version = "0.1.0"
description = "Standalone Rofi picker for Codex, Claude Code, and OpenCode sessions"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
dependencies = []

[project.optional-dependencies]
dev = ["ruff==0.16.3"]

[project.scripts]
rofi-agent-picker = "rofi_agent_picker.app:main"

[tool.setuptools.packages.find]
include = ["rofi_agent_picker*"]

[tool.ruff]
target-version = "py311"
line-length = 100
exclude = ["tests/fixtures"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
